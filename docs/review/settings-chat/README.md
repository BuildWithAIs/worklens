# Settings and chat review

Screenshots generated by the isolated browser UI tests using mock data.

- `connections.png`: shared Settings group and connection catalog.
- `activity-stream.png`: chronological narration with one latest tool status per interval.

Reproduce with Node 24: `npm run test:ui`.
