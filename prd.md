# WorkLens 第一版产品需求文档

> - 文档状态：初稿 1.2
> - 更新时间：2026 年 9 月 1 日
> - 产品阶段：第一版
> - 技术基线：Pi 软件包版本 `0.84.4`，对应源码提交 `853a80d26c90a14c1886f0ebb8ffaae133ca2185`
> - 语言说明：正文和产品术语全部使用中文；反引号内的软件包名、接口名、文件名与代码标识，以及厂商注册名称，保留官方拼写，便于研发准确对应。

本文档是第一版产品范围、行为、技术边界和验收方式的完整依据。研发人员不需要查阅任何既有 WorkLens 代码或设计文档。若实现细节与本文档冲突，以本文档及后续正式架构决策记录为准。

---

## 一、执行摘要

WorkLens 第一版是一款本地优先的通用桌面工作智能体。它不要求用户先选择代码仓库或工作目录，而是通过持久会话、本地文件工具和命令行工具，在当前操作系统用户可访问的范围内协助完成跨项目、跨目录的知识工作。

产品采用清晰的职责边界：

- WorkLens 负责产品能力：Electron 桌面应用、会话体验、设置、凭据安全、全局运行环境、进程间通信和界面。
- Pi 负责智能体能力：模型服务与模型目录、流式响应、智能体循环、工具调用、上下文压缩、重试、取消和基础本地工具。
- 第一版只做一个完整但克制的桌面智能体：用户可以配置模型、创建本地会话、让智能体访问本机不同位置的文件、执行命令，并在重启后继续会话。

Pi 的接入方式确定为：以 `@earendil-works/pi-coding-agent` 的公开接口 `createAgentSession()` 作为唯一智能体运行时，并使用同一软件包公开的 `ModelRuntime` 管理服务商目录、认证和模型可用状态。`ModelRuntime` 注入每个 `createAgentSession()`，不构成第二套智能体运行时。该软件包内部复用 `@earendil-works/pi-agent-core` 和 `@earendil-works/pi-ai`，并提供配套的会话、上下文压缩、事件流和基础工具。WorkLens 只实现产品适配层，不另行实现智能体循环、模型请求协议和基础工具协议。

第一版成功的最低标准是：用户从安装、配置、创建会话到完成一次真实本地任务，全程不需要手工选择或维护工作目录；应用重启后会话可恢复；本地工具能够访问当前操作系统用户有权访问的路径，并清楚说明第一版不提供权限模块、代码级逐次授权或文件沙箱。重要写入只通过系统提示词和工具描述，要求智能体先向用户确认。

---

## 二、产品问题与技术基线

### 2.1 产品问题

用户希望人工智能助手不仅能回答问题，还能在本地工作环境中持续完成任务。一个可用的桌面智能体需要同时解决以下问题：

- 用户需要自由选择模型服务商，而不必理解各家流式协议和工具调用差异。
- 一项任务通常跨越多轮对话，应用必须在重启后恢复完整上下文。
- 文件读取、文件修改和命令执行必须形成清晰、可追踪的工具时间线。
- 本地工具的执行范围、实际副作用，以及第一版没有权限模块、仅依靠提示词约定的风险必须向用户如实说明。
- 凭据、文件系统和命令行能力不能暴露给渲染进程。
- 首次配置、日常使用、异常恢复和应用更新都必须在桌面界面内完成。

第一版围绕上述问题建立完整闭环，不以高级功能数量作为完成标准。

### 2.2 产品技术基线

第一版采用以下技术组合：

| 层级 | 选定技术 | 职责 |
|---|---|---|
| 桌面运行框架 | Electron | 窗口、应用生命周期和安全进程边界 |
| 渲染界面 | React、TypeScript、Tailwind CSS | 会话、聊天和设置界面 |
| 智能体运行时 | `@earendil-works/pi-coding-agent` | 会话、智能体循环、上下文压缩、重试、取消和基础工具 |
| 智能体核心 | `@earendil-works/pi-agent-core` | 由上层运行时使用，不作为产品主要集成面 |
| 模型服务层 | `@earendil-works/pi-ai` | 模型目录、认证、流式响应、推理内容和工具调用协议 |
| 会话存储 | Pi `SessionManager` 与 JSONL 文件 | 持久化消息、工具调用、模型选择和上下文压缩条目 |
| 凭据存储 | Electron `safeStorage` | 加密保存模型服务凭据 |

代码统一采用 TypeScript 和 ESM。文件、网络、凭据、Pi 运行时和操作系统能力只允许出现在 Electron 主进程；渲染进程只能通过类型化预加载桥调用主进程。

### 2.3 Pi 软件包能力与限制

Pi 提供三层可复用能力：

| 软件包 | 可复用能力 | 产品用法 |
|---|---|---|
| `@earendil-works/pi-ai` | 模型目录、认证、流式响应、推理内容和工具调用协议 | 主要通过 `pi-coding-agent` 的 `ModelRuntime` 使用；只按需引用 `CredentialStore`、`AuthInteraction` 等公开契约，WorkLens 不拼装模型请求 |
| `@earendil-works/pi-agent-core` | 智能体对象、循环、事件、工具校验、取消、并发和后续消息 | 通过上层软件包间接使用，仅在必要时调用公开接口 |
| `@earendil-works/pi-coding-agent` | `createAgentSession`、`SessionManager`、上下文压缩、重试、资源加载和完整基础工具 | 第一版主要集成面 |

软件包版本 `0.84.4` 中的 `AgentHarness` 不属于可选方案。它的 `prompt()`、`compact()`、`resume()`、`abort()`、`watch()` 和钩子等关键路径仍返回 `HarnessNotImplemented`，已有记录恢复能力也未完成。与其配套的 SQLite 会话后端同样不进入第一版依赖范围。

Pi 的 `cwd` 是相对路径解析和命令启动的技术参数，不是文件访问沙箱。Pi 的基础文件工具可以使用绝对路径访问 `cwd` 之外的位置。Pi 当前没有权限模块，WorkLens 第一版也不引入。工具以当前操作系统用户权限运行。重要修改只通过系统提示词和 `write`、`edit` 的工具描述，要求智能体先向用户确认；这不是代码级拦截。未来权限模块必须作为独立于 `cwd`、session 和工具注册的策略层实现。

### 2.4 逻辑模块边界

项目至少包含以下逻辑模块。文件名可以调整，但职责和依赖方向必须保持：

| 模块 | 所在进程 | 职责 |
|---|---|---|
| 应用生命周期 | 主进程 | 创建窗口、注册协议、退出清理和单实例控制 |
| 智能体运行服务 | 主进程 | 创建与恢复多个 Pi 会话、按 session 并发发送消息、精确取消目标运行和释放资源 |
| 模型服务目录 | 主进程 | 读取 Pi 的服务商与模型目录、驱动认证、执行连接测试并维护非敏感产品状态 |
| 凭据存储 | 主进程 | 加密、读取、更新和删除凭据 |
| 会话服务 | 主进程 | 基于 Pi `SessionManager` 完成会话列表、创建、打开、重命名、删除和加载错误展示 |
| 全局运行环境 | 主进程 | 创建并维护 `~/.worklens` 数据根目录、`runtime` 运行目录和 `sessions` 会话目录 |
| 工具注册器 | 主进程 | 按操作系统注册 Pi 基础工具；不按 session 或目录裁剪工具；可为 `write`、`edit` 追加确认说明，但不增加权限拦截 |
| 事件适配器 | 主进程 | 将 Pi 事件转换为稳定的 WorkLens 事件 |
| 预加载桥 | 预加载进程 | 暴露最小类型化接口，不包含业务逻辑 |
| 会话界面 | 渲染进程 | 会话列表、消息、输入区、工具时间线和运行状态 |
| 设置界面 | 渲染进程 | 模型服务、模型、推理等级、主题和数据说明 |
| 共享契约 | 共享代码 | 进程通信输入、输出、事件和错误的唯一类型定义 |

依赖方向固定为：渲染进程调用预加载桥，预加载桥调用主进程服务，主进程服务调用 Pi 和操作系统能力。任何反向依赖都不允许。

---

## 三、产品定义

### 3.1 一句话定位

> WorkLens 是一个不受单一项目目录约束、能在用户的本地工作环境中持续完成任务并保留上下文的通用桌面工作智能体。

### 3.2 产品承诺

一个新用户应当能够完成以下闭环：

1. 安装并启动桌面应用。
2. 配置一家受支持的模型服务商。
3. 选择模型和推理等级。
4. 创建会话并提出任务。
5. 观察流式回答、推理摘要、工具调用和工具结果。
6. 在需要时直接向智能体提供文件、目录或任务位置；无需为会话选择工作目录。
7. 了解本地工具以当前操作系统用户权限运行，并可随时停止运行；重要修改可能先被智能体询问，但第一版没有权限模块。
8. 重启应用后继续原会话。

### 3.3 第一版目标

- 使用 Pi 提供模型服务、智能体循环、上下文压缩和基础工具编排。
- 交付 macOS 和 Windows 上可安装、可配置、可持续使用的完整桌面应用。
- 支持当前固定 Pi 版本提供的内置文本模型服务；WorkLens 的实测状态只作为产品标签，不决定服务商是否进入支持列表。
- 会话直接保存 Pi 消息结构及模型服务回放所需字段。
- 以应用级 `~/.worklens/runtime` 作为统一 `runtimeCwd`，但不把它解释为工作区或访问边界。
- 本地工具按产品和平台能力统一注册，不与 session 或目录绑定。
- 将首版范围控制在“桌面智能体基础闭环”。
- 为会话、工具运行和模型服务建立可自动验证的质量标准。

---

## 四、目标用户与核心场景

### 4.1 目标用户

- 需要跨代码库、文档目录和本机其他位置完成知识工作的人。
- 希望在一个桌面应用中长期保存智能体会话的人。
- 希望自由选择 Azure OpenAI、OpenAI、Anthropic 等模型服务的人。
- 希望智能体可以搜索、阅读和修改当前操作系统用户有权访问的本地内容的人。
- 不希望把接口密钥暴露给网页或渲染进程的人。

### 4.2 核心场景

#### 场景一：理解本地项目

用户输入一个仓库路径，并且输入“梳理这个项目的登录流程”。智能体使用 `ls`、`find`、`grep` 和 `read` 定位代码，持续展示进度并给出结论。整个过程只使用搜索和读取工具，不修改本地文件。

#### 场景二：完成小型修改

用户输入目标仓库路径和“给设置页增加输入校验”。智能体读取相关文件，执行 `edit` 或 `write`，并在工具时间线中展示目标路径、执行状态和结果。该路径只是本次任务上下文，不会成为 session 工作目录。

#### 场景三：运行并诊断命令

用户输入目标仓库路径和“运行类型检查并修复问题”。智能体可以让 `bash` 或 `powershell` 在命令中切换到目标路径，命令输出进入工具时间线，智能体根据结果继续处理。Shell 的初始目录为全局 `runtimeCwd`，但不限制命令访问其他位置。

#### 场景四：继续历史工作

用户重启应用并打开昨天的会话。之前的模型、推理等级、消息、工具调用和上下文压缩摘要均可恢复，然后继续提问；会话不依赖某个工作目录才能打开。

#### 场景五：跨 session 并发工作

用户在 session A 发起一个耗时任务后切换到 session B，并立即发送另一个任务。两个 session 独立流式生成和调用工具；侧边栏同时显示各自状态。用户停止 session A 时，session B 继续运行且消息、工具结果和状态不受影响。

#### 场景六：了解风险并停止运行

用户在首次使用本地工具前已看到明确风险说明。智能体可能在修改比较重要的内容前用对话征求确认；这不是授权对话框。运行中发现智能体正在进行不符合预期的操作时，用户可以停止运行；尚未开始的工具不得继续启动，已完成的工具和副作用仍保留在时间线中。第一版不把停止按钮或对话确认描述成权限模块或沙箱。

---

## 五、产品与架构原则

### 5.1 只有一套会话事实源

- Pi 的智能体消息与会话条目是对话记录的唯一事实源。
- WorkLens 界面只使用派生的展示模型，不把界面消息结构反向定义成底层协议。
- 展示模型只提取界面所需字段，不改写底层会话记录。
- 第一版不把同一份消息再复制到 WorkLens 的 SQLite 消息表。

### 5.2 只复用公开接口

- 只通过已发布软件包的公开导出项使用 Pi。
- 不从本地 `pi-mono/packages/**/src` 进行跨仓库源码引用。
- 不复制 Pi 的模型服务、智能体循环、上下文压缩或基础工具源码。
- 公开接口无法满足关键需求时，先完成技术预研或推动 Pi 增加轻量公开能力。

### 5.3 运行基准不等于安全边界

- 所有 Pi 会话统一使用 `~/.worklens/runtime` 作为 `runtimeCwd`；它只决定相对路径和 Shell 的初始位置。
- session 不保存 WorkLens 产品级工作目录，创建、打开和切换 session 都不改变 `runtimeCwd`。
- 文件工具可通过绝对路径访问当前操作系统用户有权访问的位置；`runtimeCwd` 不是目录白名单或沙箱。
- 工具在 Electron 主进程中执行，渲染进程不能直接获得 Node、文件系统或命令行能力。
- Pi 没有权限模块，WorkLens 第一版也不引入独立权限模块、授权对话框或 `waiting_authorization` 状态。
- 第一版只通过固定系统提示词，以及 `write`、`edit` 的工具描述，要求智能体在修改比较重要的内容前先用对话征求用户确认。该约定不能阻止模型直接发起工具调用；一旦发起，主进程仍直接执行。
- 未来权限模块必须作为独立于 `cwd`、session 和工具注册的策略层实现，基于具体工具、参数、解析后的绝对路径和命令判断，不得借用 `runtimeCwd` 充当权限范围。

### 5.4 模型服务能力由 Pi 决定

> Provider 能力由 Pi 决定，WorkLens 只负责安全接入和产品化。

- WorkLens 的模型服务支持集以当前固定版本 Pi 公开提供的内置文本 `Provider` 和模型目录为唯一事实源，不维护第二份服务商白名单。
- Pi 提供模型协议、认证方式、模型发现、流式响应、推理和工具调用能力；WorkLens 负责安全凭据存储、认证交互界面、连接状态、错误呈现和桌面端集成。
- “Pi 内置”和“WorkLens 已验证”是两个不同维度。未完成 WorkLens 真实账户验证的 Pi 内置服务仍在设置页中可见、可配置和可使用，只显示“尚未在 WorkLens 实测”的状态。
- 某个 Pi 内置服务只有在存在明确、可复现的桌面集成阻断时才可暂时标记为“当前不可用”，并必须显示原因和跟踪记录；不得因为缺少测试账户而静默移除。
- Pi 依赖升级是内置服务商进入或退出支持集的唯一常规方式，升级必须独立完成目录、会话和打包兼容测试。

### 5.5 首版完整性优先于功能数量

- “完整”指安装、引导、设置、会话、运行、工具、错误和恢复形成闭环。
- 不以接入更多企业工具替代基础体验的可靠性。
- 对不能在首版安全完成的能力，明确延期而不是提供半成品入口。

---

## 六、第一版范围

| 领域 | 第一版包含 | 明确延期 |
|---|---|---|
| 桌面应用 | macOS、Windows 安装包 | Linux |
| 模型服务 | 当前固定 Pi 版本的全部内置文本服务商与模型目录 | 用户自定义服务商、配置驱动且无内置模型目录的服务、图片生成服务 |
| 会话 | 新建、列表、切换、重命名、删除、恢复 | 搜索、固定、分支、导入、导出 |
| 智能体 | 流式回答、推理摘要、工具循环、取消、重试、上下文压缩、不同 session 并发运行 | 单个任务内的多智能体协作、定时任务、应用退出后后台运行、工作流编排 |
| 基础工具 | `read`、`grep`、`find`、`ls`、`write`、`edit`、`bash`、`powershell` | 浏览器、企业工具、自定义工具市场 |
| 本地执行 | 文件与命令工具以当前操作系统用户权限运行；无权限模块、目录边界或代码级逐次授权；`write`/`edit` 通过系统提示词和工具描述要求对重要修改做对话确认 | 独立权限模块、代码级逐次确认、持久规则、完整沙箱 |
| 设置 | 服务商、模型、推理等级、主题、数据与风险说明 | 自定义完整系统提示词、技能和扩展 |
| 本地数据 | Pi 会话文件、加密凭据、少量应用状态 | 长期记忆、全文索引、工作焦点 |
| 内容 | 文本、Markdown、代码块 | 图片、网页执行预览 |

---

## 七、信息架构与界面

### 7.1 一级结构

桌面窗口只保留三个主要区域：

1. 会话侧边栏。
2. 智能体对话区。
3. 设置页。

长期记忆页和工作看板不进入第一版导航。

### 7.2 会话侧边栏

必须提供：

- 新建会话。
- 按最近活动时间排列的会话列表。
- 当前会话高亮。
- 会话标题、模型和最近更新时间。
- 重命名和删除入口。
- 每个会话各自的运行状态；多个 session 同时运行时都必须可见。
- 打开设置入口。

不提供搜索、文件夹、标签、固定和工作看板入口。

### 7.3 智能体对话区

必须提供：

- 顶部上下文栏：模型服务商、模型、推理等级和本地工具状态。
- 消息区：用户消息、助手文本、可折叠推理摘要、工具步骤和错误状态。
- 输入区：当前 session 空闲时显示发送，当前 session 运行时显示停止；其他 session 是否运行不影响本 session 发送。
- 运行状态：正在生成、正在执行工具、正在压缩和正在重试。
- 首次使用本地工具前的能力与风险说明。
- 不要求选择目录即可开始的空会话引导。

### 7.4 设置页

至少包含：

- 模型服务账户与连接状态。
- Pi 为当前服务商声明的接口密钥、OAuth、设备码、环境凭据或其他认证入口。
- 默认模型服务商、模型和推理等级。
- 浅色、深色和跟随系统主题。
- `~/.worklens` 数据根目录、`runtime` 运行基准、`sessions` 会话位置、版本、工具能力和隐私说明。

### 7.5 视觉与交互语言

产品采用“暖静”视觉语言：低对比暖色背景、克制的青绿色强调色、清晰但不过度突出的层级。界面应让长时间阅读和工具执行过程保持平静、可信。

基础颜色如下：

| 语义 | 浅色主题 | 深色主题 |
|---|---|---|
| 应用背景 | `#f6f4ef` | `#14120e` |
| 侧边栏 | `#efebe3` | `#1a1611` |
| 内容表面 | `#fffdfa` | `#211d18` |
| 主文字 | `#211d18` | `#ece7dd` |
| 次文字 | `#494139` | `#c3bbae` |
| 强调色 | `#0f8478` | `#3fc2b0` |
| 成功色 | `#1f8a5b` | `#35bd7e` |
| 警告色 | `#b3791b` | `#dca23f` |
| 危险色 | `#c45440` | `#e0705a` |

实现要求：

- 所有颜色通过语义化样式变量使用，组件中不直接写颜色值。
- 浅色和深色主题使用同一套语义变量，每个新增变量必须同时定义两种主题。
- 圆角只使用 10、12、16 和 20 像素四档。
- 间距以 8 像素为基础节奏，允许使用 4 像素作为半档。
- 正文字体优先使用系统字体，并为中文配置苹方和思源黑体回退。
- 代码使用等宽字体，并在两种主题下保持可读对比度。
- 阴影只用于浮层和需要区分层级的表面，不用于普通列表项。
- 动画只表达状态变化，持续时间控制在 120 至 240 毫秒；系统启用减少动态效果时关闭非必要动画。
- 危险操作不能只依靠红色表达，必须同时提供图标、标题和明确文案。

---

## 八、详细功能需求

以下需求中的“最高”表示首版发布阻断项。

### 8.1 首次启动与空状态

#### 需求 001：首次启动引导（最高）

没有可用模型服务时，应用显示可完成的引导流程，而不是不可用的空聊天页。

流程为：

1. 选择模型服务商。
2. 按 Pi 为该服务商声明的认证流程完成接口密钥、OAuth、设备码、环境凭据或其他必要配置。
3. 选择模型和推理等级。
4. 阅读本地工具将以当前操作系统用户权限运行的说明，包括第一版没有权限模块，以及重要写入只通过提示词约定先确认。
5. 直接发送第一条消息；如果任务涉及本地资源，可以在消息中提供路径。

用户可以退出引导并进入设置，但不能在未配置模型时发送消息。

#### 需求 002：无需工作目录即可开始（最高）

WorkLens 不提供“选择工作目录”步骤，也不存在“未选择工作目录”状态。只要模型可用，用户即可创建 session 并使用产品级统一注册的本地工具；工具是否可用不取决于用户是否提供过文件或目录路径。

### 8.2 模型服务、模型与凭据

#### 需求 010：Pi 模型服务支持集（最高）

- 第一版的支持集来自注入 WorkLens `CredentialStore` 后创建的 Pi `ModelRuntime`。主进程通过其公开接口读取 `getProviders()`、`getModels()`、`getAvailable()` 和刷新结果，不在 WorkLens 中复制服务商或模型清单。
- 主进程维护应用级共享 `ModelRuntime`，并将同一实例注入所有 `createAgentSession()`；设置页、连接测试和会话运行不得各自创建互相漂移的 provider 运行时。
- 设置页展示当前固定 Pi 版本中具有内置文本模型目录的全部服务商。配置驱动且没有内置模型目录的服务、用户自定义服务商和图片生成服务不属于第一版文本智能体范围。
- 服务商标识、显示名称、认证方式、模型及模型能力以 Pi 返回值为准。WorkLens 不得用手工维护的六家或其他白名单裁剪 Pi 支持集。
- Pi 新增、删除或调整内置服务商时，WorkLens 通过独立的 Pi 版本升级同步变化；运行时不得远程替换已固定软件包中的服务商实现。
- 每个服务商分别显示两个正交的非敏感状态：配置与连接状态为“未配置”“已配置”“连接失败”等；产品验证状态为“Pi 内置、WorkLens 尚未实测”或“WorkLens 已验证”。验证状态影响标签、排序和发布说明，不影响其是否可配置和使用。
- 若某服务在 Electron 环境存在明确阻断，仍保留在列表中并标记“当前不可用”，显示经过脱敏的原因；修复应优先进入 Pi 或通用 WorkLens 适配层，不得新增服务商专用请求实现。

#### 需求 011：服务商产品覆盖层（最高）

WorkLens 可以维护轻量、可选的 `ProviderPresentationOverride`，但它只允许描述：

- 中文名称、排序、分类和帮助文档。
- WorkLens 实测状态、验证证据版本和明确阻断原因。
- Pi 通用认证交互尚未声明的附加产品字段，例如 Azure 基础地址、资源名、接口版本和部署映射。

服务商成员关系、认证方法、模型发现方式和模型能力不得由该覆盖层重新定义。没有覆盖项的 Pi 内置服务必须使用 Pi 元数据正常显示和配置；覆盖层不得实现网络协议或模型请求。

#### 需求 012：凭据安全（最高）

- 主进程实现 Pi 要求的 `CredentialStore`。
- 凡由 WorkLens 持久化的接口密钥、OAuth 令牌和 provider 环境配置，都通过 Electron `safeStorage` 加密后落盘；AWS 凭据链、Google ADC 等外部凭据来源只由 Pi 按其认证规则使用，WorkLens 不复制其秘密内容。
- `CredentialStore.modify()` 按服务商串行执行，避免并发刷新覆盖令牌。
- 渲染进程常规设置数据只包含“已配置”、认证类型和连接状态，不包含明文凭据。
- 第一版不提供明文查看；替换密钥需要重新输入。
- 日志、错误、进程通信事件和遥测不得包含令牌。

#### 需求 013：模型目录和能力展示（最高）

- 服务商、模型目录和可用状态来自同一个 Pi `ModelRuntime`，不得分别维护或拼接另一份清单。
- 界面至少展示模型名称、服务商、是否支持推理、是否支持图片输入和上下文窗口大小。
- 图片输入能力仅用于信息展示；第一版不提供图片上传。
- 推理等级只能选择 Pi 对当前模型允许的值。
- 动态模型列表必须有超时、缓存和失败回退。

#### 需求 014：Azure 部署映射（最高）

Azure OpenAI 的模型标识与部署名称不能假设相同。设置页必须允许用户填写基础地址或资源名、可选接口版本以及模型到部署名称的映射，并通过 Pi 支持的 provider 环境配置或公开请求选项传入；这只是产品配置覆盖层，不得形成 Azure 专用请求客户端。

验收要求：自定义部署名称的 Azure 实例可以通过连接测试，并完成至少一轮含工具调用的对话。

#### 需求 015：测试连接（最高）

- 使用 Pi 发起一次受限的真实模型请求。
- 限制输出令牌数量和超时时间。
- 成功时显示实际使用的服务商和模型。
- 失败时区分认证、模型不可用、服务端点、限流、网络和超时问题。
- 测试内容不得读取任何本地文件或调用本地工具。

#### 需求 016：会话模型设置（最高）

- 新会话继承全局默认服务商、模型和推理等级。
- 用户可在会话空闲时切换当前模型和推理等级。
- 运行期间禁止切换，避免事件与状态错配。
- 会话选择通过 Pi 会话条目持久化。
- 共享 `ModelRuntime` 只提供服务商目录、认证和模型能力，不能保存应用级“当前模型”。每个 `SessionRuntime` 独立持有会话模型设置，并发 session 可以使用不同服务商、模型和推理等级。

### 8.3 全局运行目录

#### 需求 020：初始化全局 `runtimeCwd`（最高）

- 正式版主进程在启动时通过 `os.homedir()` 定位用户主目录，并建立固定的 `worklensRoot = ~/.worklens`、`runtimeCwd = ~/.worklens/runtime` 和 `sessionDir = ~/.worklens/sessions`；开发、内部测试和自动化环境按第 19.1 节使用隔离根目录。
- Windows 典型路径为 `C:\Users\<user>\.worklens\runtime` 与 `C:\Users\<user>\.worklens\sessions`，macOS 典型路径为 `/Users/<user>/.worklens/runtime` 与 `/Users/<user>/.worklens/sessions`；实现不得把示例路径写死。
- 根目录、运行目录或会话目录不存在时由主进程创建；目标已存在但不是目录、无法创建或无法访问时，应用显示可诊断的启动错误，不得静默退回用户主目录、当前进程目录或任意 session 路径。
- `runtimeCwd` 作为显式 `cwd` 注入所有 Pi `SessionManager`、`DefaultResourceLoader` 和 `createAgentSession()` 调用。应用运行期间只有一个该值。
- `~/.worklens/runtime` 只承载相对路径解析和 Shell 初始位置；`~/.worklens/sessions` 只存放 Pi 会话 JSONL。凭据、设置、缓存和日志继续使用 Electron `userData`。

#### 需求 021：session 与目录解耦（最高）

- WorkLens 的 session 模型不包含 `workingDirectory`、`workspacePath` 或等价产品字段。
- 新建、打开、切换和恢复 session 都使用同一个全局 `runtimeCwd`，不会显示目录选择器，也不会因为任务路径变化创建新 session。
- 用户在消息中提供的文件或目录路径只是会话内容，不会改变 session 身份、工具注册或全局 `runtimeCwd`。
- Pi JSONL 会话头中因运行时格式要求保留的 `cwd` 统一写为 `~/.worklens/runtime` 的规范化绝对路径；WorkLens 不展示、不索引，也不将其解释为 session 工作目录。
- 打开历史数据时，如果 Pi 会话头中的 `cwd` 与当前全局值不同，主进程必须通过 Pi 公开能力覆盖为当前 `runtimeCwd` 或执行兼容迁移，不能恢复旧的逐会话目录语义。

### 8.4 会话管理

#### 需求 030：新建会话（最高）

- 点击“新建会话”立即打开空白草稿。
- 空白草稿只存在于界面状态，不提前创建空文件。
- 首条消息发送后，由 Pi `SessionManager` 建立持久会话。
- 创建时记录初始模型设置；运行时统一使用应用级 `runtimeCwd`。

#### 需求 031：会话列表（最高）

- 通过 Pi `SessionManager` 从固定的 `~/.worklens/sessions` 目录读取 Pi 会话文件。
- 首次显示目标覆盖至少 500 个会话。
- 列表项包含会话标识、标题、更新时间和模型，不显示或按目录分组。
- JSONL 文件是对话记录唯一事实源；不建立第二份消息数据库。
- 会话发现、JSONL 解析、格式迁移和有效性判断直接采用当前固定 Pi 版本的 `SessionManager` 行为；单个无法识别或无法打开的文件不能阻止其他会话加载。

#### 需求 032：会话标题（最高）

- 默认标题来自首条用户消息的本地截断，不额外调用模型。
- 用户可以重命名。
- 标题通过 Pi 会话信息写入会话文件，而不是只存于界面状态。

#### 需求 033：打开与恢复（最高）

重新打开会话时必须恢复：

- 用户、助手和工具结果消息。
- 工具调用参数和执行状态。
- 模型服务商、模型和推理等级。
- 上下文压缩摘要。

如果会话所用模型不再可用，应用保留历史记录并要求用户选择其他模型，不自动改写历史消息。

#### 需求 034：删除会话（最高）

- 删除前显示标题与不可恢复提示。
- 目标 session 正在运行时，必须先停止该 session 的运行；其他 session 的运行不受影响。
- 主进程确认目标文件位于 WorkLens 专属会话目录内，且文件内标识与请求标识一致。
- 只删除目标会话文件，不使用通配符或递归删除。

#### 需求 035：并发规则（最高）

- 不同 session 彼此独立，可以同时运行智能体。一个 session 已发送消息并处于生成或工具执行状态时，用户切换到另一个空闲 session，仍然可以立即发送消息并启动新的运行。
- 同一个 session 同一时间最多只有一个活跃运行。当前 session 运行期间不能再次发送普通消息；用户可以停止该运行，待其完成收束后再发送下一条消息。
- 主进程按 `conversationId` 保存 `SessionRuntime`，其中包含对应 `AgentSession` 和可选的 `ActiveRun`；每次运行分配应用级唯一 `runId`。不得使用单个全局 `activeRun`、全局忙碌布尔值或当前界面选中的 session 表示运行归属。
- 切换页面或 session 不暂停、不重建也不取消后台 session 的运行。切回后必须能继续看到该 session 已经产生的流式内容、工具步骤和最终状态。
- 会话侧边栏分别显示每个 session 的运行状态。停止、重试、错误和完成状态只作用于所属的 `conversationId` 与 `runId`，不得影响其他并发 session。
- 第一版不设置产品级“同时只能运行 N 个 session”的硬上限。模型服务商限流、操作系统资源不足等错误按所属 session 独立呈现，不能把其他 session 的输入区整体锁定。

### 8.5 对话与运行

#### 需求 040：文本消息（最高）

- 支持多行纯文本输入。
- 用户消息被主进程接受后立即显示。
- 每次运行使用稳定的 `runId` 和 `conversationId`。
- `runId` 在应用范围内唯一；主进程拒绝同一个 `conversationId` 上的第二个活跃运行，但允许其他 `conversationId` 同时启动。
- 重复点击发送不得创建重复运行。
- 空消息和只有空白的消息不能发送。

#### 需求 041：流式回答（最高）

WorkLens 将 Pi 的 `AgentSessionEvent` 映射为稳定的桌面事件，至少覆盖：

- 智能体开始与结束。
- 轮次开始与结束。
- 消息开始、增量和完成。
- 工具开始、参数增量和完成。
- 上下文压缩开始与完成。
- 重试开始与完成。
- 运行取消、失败和收束。

每个事件必须携带 `runId` 和 `conversationId`。渲染进程不得根据“当前打开的会话”猜测事件归属。

并发 session 的事件可以交错到达；事件适配器和界面状态必须先按 `conversationId` 定位会话，再按 `runId` 校验当前运行。迟到或重复事件只能影响其原始运行。

#### 需求 042：推理摘要展示（最高）

- 模型返回 `thinking_delta` 时，以默认折叠区域展示。
- 界面统一称为“推理摘要”，不声称它是模型完整思维链。
- 模型不返回推理内容时不显示空容器。
- 推理摘要与最终回答在视觉和数据上分离。

#### 需求 043：工具时间线（最高）

每次工具调用显示：

- 工具名称。
- 参数摘要。
- 当前状态：待执行、等待资源、执行中、成功、失败或已取消。
- 开始时间和耗时。
- 有界的结果摘要。
- `edit` 提供的差异或补丁预览。

工具参数在流式生成期间只用于展示，不得在参数完成并通过校验前执行。

#### 需求 044：停止运行（最高）

- 生成期间发送按钮变为停止按钮。
- 停止操作必须通过 `conversationId` 和当前 `runId` 定位目标 Pi `AgentSession`，并只对该实例调用 `abort()`。
- 模型请求和工具共享同一取消语义。
- 界面最终进入“已取消”，不能永久停留在“生成中”。
- 已完成的工具结果和已发生的副作用仍保留在会话中。
- 停止、关闭或删除一个 session 不得取消、清空或覆盖其他 session 的活跃运行。

#### 需求 045：错误与重试（最高）

- 模型服务错误保留在会话中，并显示可以理解的恢复建议。
- 使用 Pi 的受限自动重试，显示等待时间和次数。
- 用户可在等待期间取消。
- 不对认证错误、无效请求和不可重试的工具参数错误自动重试。
- 手动重试必须形成新的运行标识。
- 限流、重试等待和错误状态按 `runId` 独立管理；一个 session 的重试计时不得阻塞其他 session 发送或推进。

#### 需求 046：上下文压缩（最高）

- 上下文压缩直接使用 Pi `AgentSession` 提供的公开能力。
- 自动压缩和上下文溢出恢复都显示状态。
- 压缩结果进入 Pi 会话，并在重启后恢复。
- 压缩失败不能静默丢弃历史消息。
- 第一版不额外生成会话摘要或长期记忆。

#### 需求 047：Markdown 与代码（最高）

- 最终回答支持 Markdown、常用扩展语法、代码高亮和复制。
- 模型生成的 HTML 只作为代码显示，第一版不执行网页预览。
- 外部链接通过主进程受控调用 `openExternal` 打开。
- 渲染内容必须经过清理，不能注入应用脚本或进程通信桥。

### 8.6 工具与执行边界

#### 需求 050：产品级工具注册（最高）

第一版不提供按 session 切换的“只读”或“完整本地工具”模式。工具由 WorkLens 根据 Pi 公开能力和当前操作系统统一注册：

| 平台 | 默认注册工具 | 执行规则 |
|---|---|---|
| Windows | `read`、`grep`、`find`、`ls`、`write`、`edit`、`powershell` | 由 Pi 发起后在主进程直接执行 |
| macOS | `read`、`grep`、`find`、`ls`、`write`、`edit`、`bash` | 由 Pi 发起后在主进程直接执行 |

同一平台上的所有 session 使用同一工具集。工具注册不得取决于 session、`runtimeCwd`、用户是否输入过路径或当前路径所在磁盘。未来新增工具时仍按产品能力注册；即使以后引入权限模块，权限策略也只能决定一次具体调用能否执行，不能重新建立“目录决定工具是否存在”的耦合。

注册 `write` 和 `edit` 时，必须在不改变 Pi 公开参数、执行路径和结果契约的前提下，向工具描述追加一句简短说明：如果修改比较重要的内容，应先通过对话征求用户确认。不得为此包装第二套工具实现，也不得把描述变成拦截逻辑。若 Pi 不允许改写描述，同一句话仍必须出现在固定系统提示词中。

#### 需求 051：操作系统范围的文件访问（最高）

- 相对路径以全局 `runtimeCwd` 解析；绝对路径保持其操作系统语义，不强制拼接到 `~/.worklens/runtime` 下。
- 文件工具可以访问当前 WorkLens 进程所属操作系统用户有权访问的文件、目录、其他磁盘和网络路径。
- 第一版不实现基于目录的允许列表、父目录边界或符号链接逃逸拦截。操作系统 ACL、文件锁、UAC、macOS TCC 等产生的拒绝以工具错误返回给智能体。
- `runtimeCwd` 只是默认起点，不是文件访问范围、项目根目录或安全沙箱。界面、提示词和错误文案不得暗示存在上述限制。

#### 需求 052：文件写入（最高）

- `write` 和 `edit` 使用 Pi 的公开工具契约和参数校验，并以当前操作系统用户权限直接执行。
- 工具时间线展示目标绝对路径、操作类型、执行状态、结果摘要，以及 Pi 能提供的差异或补丁。
- 覆盖、权限不足、目标不存在和磁盘错误必须形成明确且互不混淆的工具结果。
- 第一版不引入写入前的权限模块、授权对话框或代码级拦截。模型一旦发起 `write` 或 `edit`，主进程直接执行。
- 固定系统提示词以及 `write`、`edit` 的工具描述必须要求智能体：用户已经明确要求的修改可以直接执行；若修改比较重要的内容，例如用户未要求改动的已有文件、凭据或配置、大量覆盖或删除，应先在对话中说明意图并等待用户明确同意，再调用工具。该要求是行为指引，不是安全边界。
- 首次使用本地工具前必须明确说明：智能体可能创建、修改或覆盖用户有权访问的文件；提示词约定不能替代权限模块。

#### 需求 053：命令行执行（最高）

- `bash` 或 `powershell` 的初始目录为全局 `runtimeCwd`，命令可以使用绝对路径、切换目录、访问其他磁盘或调用当前用户可执行的程序。
- 工具时间线展示完整命令、初始目录、超时时间、退出码、执行状态和有界输出。
- 命令以当前操作系统用户权限直接运行，并非沙箱。第一版没有执行前的权限模块或代码级逐次确认，也不尝试通过字符串分析限制命令影响范围。
- 系统提示词可以对会造成重要修改或破坏性副作用的命令提出与 `write`、`edit` 相同的对话确认要求，但不为 `bash` 或 `powershell` 增加拦截层。
- 首次使用本地工具前必须明确说明命令可能修改文件、启动进程、访问网络或产生其他系统副作用。

#### 需求 054：统一工具执行入口（最高）

- Pi 当前没有权限模块，WorkLens 第一版也不实现授权对话框、授权进程间通信、`waiting_authorization` 状态、逐次允许规则或任何权限拦截。对话中的二次确认只来自模型遵循提示词，不经过该执行入口的放行逻辑。
- 所有本地工具仍必须由主进程中的统一执行入口调用，不能散落到渲染进程或按 session 创建不同执行路径。
- 统一执行入口应保留未来插入 `ToolExecutionPolicy` 的边界，但第一版不得把该边界实现为实际的允许、拒绝或询问逻辑，也不得把系统提示词约定实现成策略层。未来策略输入至少包含工具名、完整参数、解析后的绝对路径、命令、`runId` 和 `conversationId`。
- 未来权限模块不得改变全局 `runtimeCwd`，不得把工具注册绑定到目录，也不得把 session 重新定义为工作区。

#### 需求 055：工具并发（最高）

- 同一个 run 中的搜索和读取工具可以按 Pi 的公开并发语义执行。
- 同一个 run 中的 `write`、`edit`、`bash` 和 `powershell` 顺序执行，避免单个智能体在同一任务内制造不可预测的副作用顺序。
- 不同 session 的工具调用不共享应用级全局执行锁；两个 session 可以同时进行模型请求、读取、写入或命令执行。
- 不同 session 对同一个规范化目标文件执行 `write` 或 `edit` 时，主进程按目标路径使用短期互斥锁，避免两个文件写操作在字节级重叠。等待锁的工具显示“等待资源”，取消后立即退出等待队列。
- 路径互斥只保证一次文件工具调用完整执行，不提供事务隔离或业务合并；后执行的 `write` 仍可能覆盖先前结果，`edit` 的预期内容不再匹配时必须明确失败。
- `bash` 和 `powershell` 可能修改任意资源，WorkLens 无法可靠推断其冲突范围，因此不同 session 的命令可以并发执行且不宣称具备资源隔离。风险说明必须建议用户避免让多个 session 同时修改同一目标。
- 取消一个 run 后，该 run 尚未开始或仍在等待资源的工具不得启动；其他 session 的工具队列和执行不受影响。

#### 需求 056：工具输出（最高）

- 复用 Pi 基础工具的截断与结果结构。
- 界面只渲染有界摘要，长输出不能冻结页面。
- 工具输出不得包含凭据。
- 如果完整输出被写入临时文件，渲染进程不直接获得任意临时路径访问权；智能体只能通过受控工具继续读取。
- 命令退出码、超时和取消必须形成不同状态。

### 8.7 设置与应用行为

#### 需求 060：默认设置（最高）

设置页允许配置：

- 默认模型服务商。
- 默认模型。
- 默认推理等级。
- 浅色、深色或跟随系统主题。

模型默认值只影响新会话；当前会话的模型设置按需求 016 处理。主题设置即时生效。设置页不提供 session 工作目录、默认工作目录或按目录裁剪工具的选项。

#### 需求 061：固定系统提示词（最高）

第一版使用 WorkLens 内置系统提示词，并通过显式配置交给 Pi `ResourceLoader`。用户不能修改完整系统提示词。

系统提示词必须：

- 说明桌面智能体职责。
- 说明全局 `runtimeCwd` 只是相对路径和命令的起点，不是工作区或沙箱。
- 列出可用工具，并说明它们可以访问当前操作系统用户有权访问的位置。
- 说明 `~/.worklens/sessions` 保存 WorkLens 内部会话记录，除非用户明确要求处理该目录，否则不要主动搜索、读取或修改；该说明是行为指引，不是代码级访问限制。
- 要求智能体在调用 `write` 或 `edit` 修改比较重要的内容前，先通过对话向用户确认；用户已经明确要求的修改可以直接执行。
- 明确第一版没有权限模块，上述确认只是提示词约定，不能向用户宣称存在授权对话框、逐次放行或代码级保护。
- 要求在工具调用之间给出简短进度。
- 不宣称具有已延期的长期记忆或企业集成能力。

#### 需求 062：关闭资源自动发现（最高）

创建 Pi `DefaultResourceLoader` 时必须关闭：

- 第三方扩展。
- 技能。
- 提示词模板。
- 主题资源。
- 项目内 `AGENTS.md` 和 `CLAUDE.md` 的自动发现。

第一版不得隐式扫描或执行第三方资源。即使 `runtimeCwd` 固定为 `~/.worklens/runtime`，其中或 `~/.worklens` 其他子目录中出现的提示词、扩展、技能、`AGENTS.md` 或 `CLAUDE.md` 也不能被自动加载。

#### 需求 063：应用生命周期（最高）

- 应用退出前遍历并取消所有 session 的活跃运行和尚未开始的工具。
- 清理运行时监听器与临时资源。
- 不在退出时丢弃已完成的消息或工具结果。
- 异常退出后，下次启动仍通过 Pi `SessionManager` 发现和打开可识别的会话。
- WorkLens 第一版不增加独立 JSONL Schema 校验、完整性签名、自动修复或隔离器。Pi 无法识别或打开某个文件时，WorkLens 捕获错误并显示诊断信息；该错误不能阻止应用启动或其他会话使用。

---

## 九、Pi 接入决策

### 9.1 选定方案

以 `@earendil-works/pi-coding-agent` 的 `createAgentSession()` 作为主要运行时。

```text
WorkLens 智能体运行服务
  ├─ WorkLens 凭据存储      -> Pi 模型运行时
  ├─ WorkLens 全局运行环境  -> ~/.worklens/runtime
  ├─ WorkLens 资源加载器    -> 固定提示词并关闭自动发现
  ├─ WorkLens 工具注册器    -> 与 session 和目录无关的平台工具集
  ├─ Pi 会话管理器          -> ~/.worklens/sessions/*.jsonl
  ├─ Pi 智能体会话          -> 循环、工具、压缩、重试和取消
  └─ 智能体事件适配器       -> 类型化 Electron 进程间通信事件
```

选择理由：

- 这是 Pi 文档明确面向自定义网页和桌面界面的公开接口。
- 可以同时复用智能体循环、模型服务、会话、上下文压缩和基础工具。
- `AgentSession` 在消息结束事件上持久化记录；带工具调用的助手消息可在工具执行前形成持久化边界。
- 会话保留 Pi 原生 `AgentMessage`，降低模型回放字段丢失风险。
- 事件模型可以覆盖需求 041 定义的流式事件。

### 9.2 不采用仅使用核心智能体对象的方案

只使用 `pi-agent-core.Agent` 会迫使 WorkLens 重新实现：

- 会话格式、持久化和恢复。
- 上下文压缩生命周期。
- 重试和上下文溢出恢复。
- 完整基础工具或工具适配器。
- 模型和推理等级变更的持久化。

这与复用 Pi 公开能力的架构目标相悖。只有当 `pi-coding-agent` 在 Electron 中的打包成本不可接受时，才重新评估该降级方案。

### 9.3 不采用未完成的智能体框架

软件包版本 `0.84.4` 中，`AgentHarness` 的关键功能仍明确未实现，配套 SQLite 后端面向该框架，无法承载首版。不得将未完成接口包装后投入产品。

### 9.4 不采用独立伴随进程

第一版不通过独立 Pi 子进程或远程过程调用接入，原因是：

- 增加进程生命周期、通信和打包复杂度。
- 凭据、会话和工具执行需要跨进程重新设计。
- Electron 主进程已经可以承载 Node 运行时。

未来若需要更强隔离、远程运行或独立崩溃恢复，可以重新评估伴随进程方案。

### 9.5 依赖策略

- 发布构建固定公开软件包版本，首个候选版本为 `0.84.4`，或技术预研确认后的更新版本。
- 本地文件依赖和工作区链接只用于预研，不进入正式发行配置。
- 工具参数和结果采用 Pi 公开的 `typebox` 类型契约，WorkLens 不定义并行的工具类型体系。
- 每次升级 Pi 都必须运行模型服务、会话、工具、进程通信和打包兼容测试。
- 禁止引用 Pi 仓库内部未导出的源码路径。

---

## 十、进程边界与通信

### 10.1 Electron 主进程

主进程负责：

- Pi 运行时的创建、恢复和销毁。
- 按 `conversationId` 管理多个并发 `AgentSession` 和各自的活跃 `runId`。
- 所有模型网络请求。
- 凭据加解密。
- 会话文件读写。
- 创建和维护 `~/.worklens`、全局 `~/.worklens/runtime` `runtimeCwd` 与 `~/.worklens/sessions` 会话目录。
- 注册并执行产品级本地工具。
- 外部链接打开。
- 事件归一化和进程间通信。

### 10.2 预加载桥

预加载层只暴露最小、类型化接口。建议的逻辑分组如下；名称是实现标识，不是产品文案：

```ts
window.api.providers.list()
window.api.providers.getStatus()
window.api.providers.saveCredential(input)
window.api.providers.login(input)
window.api.providers.logout(providerId)
window.api.providers.test(input)
window.api.providers.listModels(providerId)

window.api.conversations.list()
window.api.conversations.create(input)
window.api.conversations.open(conversationId)
window.api.conversations.rename(input)
window.api.conversations.delete(conversationId)

window.api.chat.send(input)
window.api.chat.cancel({ conversationId, runId })
window.api.chat.onEvent(listener)

window.api.system.openExternal(url)
```

所有输入在主进程再次校验，不能信任渲染进程传入的会话标识、模型标识或外部链接。预加载桥不暴露任意文件读写、命令执行或目录选择接口；本地工具只能由主进程中的 Pi 智能体运行链路发起。

### 10.3 渲染进程

渲染进程不得：

- 直接导入 Electron 或 Node 模块。
- 直接读取会话文件。
- 直接请求模型服务。
- 接触明文凭据。
- 直接调用文件、命令行或其他本地工具。
- 修改全局 `runtimeCwd` 或为 session 注入目录状态。

### 10.4 session 级运行状态

主进程维护 `Map<conversationId, SessionRuntime>`，而不是应用级唯一活跃运行。`SessionRuntime` 持有对应 `AgentSession`、事件订阅，以及零个或一个 `ActiveRun`。每个 session 独立遵循以下状态机：

```text
空闲
  -> 正在请求模型
  -> 正在执行工具
  -> 正在请求模型
  -> 完成、失败或取消
```

`ActiveRun` 至少包含 `conversationId`、`runId`、当前状态和取消句柄。同一个 `conversationId` 只能有一个 `ActiveRun`，不同 `conversationId` 的状态机可以同时推进。重复打开正在运行的 session 必须复用已有 `SessionRuntime`，不能创建第二个指向同一会话文件的 Pi 实例；空闲实例可以按资源策略释放并通过同一会话文件恢复。

每次状态变化都带运行标识与会话标识。已经结束的运行所产生的迟到事件不得覆盖该 session 的后续运行，也不得写入其他 session。界面当前选中的 session 只是视图状态，不能决定事件路由和运行生命周期。

---

## 十一、数据与持久化

### 11.1 会话

第一版使用 Pi `SessionManager` 的 JSONL 文件作为对话记录唯一事实源，存放于固定的 `~/.worklens/sessions` 目录。WorkLens 将该路径作为显式 `sessionDir` 传给所有 Pi 会话，不让 Pi 根据 `cwd` 派生会话目录。

会话至少包含：

- Pi 会话头；其中运行时要求的 `cwd` 固定为当前全局 `runtimeCwd`，不作为 WorkLens 产品字段。
- 用户、助手与工具结果消息。
- 模型变更。
- 推理等级变更。
- 上下文压缩条目。
- 会话标题等信息。

WorkLens 不得将 Pi 消息转换后重复写入另一张消息表。

### 11.2 应用状态

少量非敏感界面状态存放在 `app-state.json`：

- 最近打开的会话标识。
- 默认模型服务商、模型和推理等级。
- 主题。
- 引导是否完成。
- 本地工具风险说明是否已展示。

如果大量会话导致扫描性能不足，可以增加可重建的 SQLite 索引。索引只能加速列表，不能成为对话记录的第二事实源。

### 11.3 凭据与模型设置

- 凭据加密存储，只在主进程解密。
- 非敏感设置包含服务商标识、模型标识、推理等级和 Azure 部署映射。
- OAuth 访问令牌和刷新令牌均视为凭据。
- Pi 的动态模型缓存放在 WorkLens 专属 `userData` 中，不默认读取用户已有的 `~/.pi` 配置。

### 11.4 删除与数据保留

- 用户确认删除会话后，目标会话文件被永久删除。
- 删除凭据不删除会话；重新打开时要求配置可用模型。
- 卸载后是否保留 Electron `userData` 遵循操作系统和安装器默认行为；安装器不得自动递归删除 `~/.worklens`，其中包含会话历史和工具以相对路径创建的用户文件。
- 第一版设置页分别展示 `~/.worklens/sessions` 会话位置和 Electron `userData` 设置、凭据、缓存与日志位置。

### 11.5 崩溃一致性

副作用工具执行前，必须已经持久化包含工具调用的助手消息；工具完成后，应尽快持久化工具结果。

技术预研若发现“副作用已经发生但会话中没有任何记录”的窗口，必须在发布前增加 WorkLens 自己的轻量运行日志。该日志只补足崩溃恢复，不得演变成第二套对话数据库。

并发 session 分别写入各自的 JSONL 会话文件，不共享可变消息缓冲。运行日志中的每条记录必须同时包含 `conversationId` 和 `runId`；一个 session 的异常写入或恢复不得阻塞其他 session 持久化。

---

## 十二、Pi 模型服务接入与验证策略

### 12.1 “支持”的定义

“支持”表示当前固定 Pi 版本通过公开 `Provider`、模型目录和 `ModelRuntime` 暴露该文本模型服务，且不属于第一版明确延期的自定义或图片生成范围。WorkLens 必须提供通用入口，不以真实测试账户、独立白名单或产品覆盖项作为进入条件。

支持集遵循以下规则：

- Pi 的 `getProviders()` 和 `getModels()` 是服务商与模型成员关系的事实源。
- WorkLens 使用 Pi 的 `login()`、`logout()`、`checkAuth()`、`refresh()` 和模型请求路径，不实现服务商专用认证或流式协议。
- 没有 `ProviderPresentationOverride` 的服务使用 Pi 原始名称、认证能力和模型元数据正常工作。
- 缺少真实账户只会使状态保持“WorkLens 尚未实测”，不会移除或禁用该服务。
- 明确的 Electron 集成故障显示为“当前不可用”，并作为需要修复的兼容问题处理，不把故障解释为 WorkLens 自行缩小支持范围。

### 12.2 “WorkLens 已验证”的定义

“WorkLens 已验证”是比“Pi 内置”更强的产品质量标签。只有使用真实账户在当前 WorkLens 与 Pi 版本组合上通过以下项目，才可以显示该标签：

- 首次配置和重新配置。
- 凭据错误与脱敏。
- 文本流式回答。
- 至少一轮工具调用。
- 取消正在进行的模型请求。
- 限流、网络失败和超时提示。
- 推理能力与可选等级正确展示。
- 会话重开后继续对话。
- macOS 和 Windows 安装包内运行。

验证证据必须记录 WorkLens 版本、Pi 版本、服务商、模型、平台和日期。验证结果用于状态标签、推荐排序和发布说明；没有标签的 Pi 内置服务仍可配置和使用。

### 12.3 认证和产品适配

- WorkLens 以通用界面驱动 Pi `AuthInteraction` 的文本、密码、选择、手工代码、OAuth 地址、设备码和进度事件。
- 接口密钥和 OAuth 凭据统一通过 WorkLens `CredentialStore` 安全落盘，Pi 负责认证解析、OAuth 刷新和请求鉴权。
- AWS 凭据链、Google ADC、企业域名等 Pi 声明的认证路径由同一通用交互承载；WorkLens 只增加路径选择、风险说明和非敏感状态展示。
- Azure 等需要额外 provider 环境配置的服务可以使用 `ProviderPresentationOverride` 收集 Pi 尚未声明为通用认证提示的字段，但不能绕开 Pi 发送请求。

### 12.4 GitHub Copilot 与企业域名

标准 GitHub Copilot 是 Pi 内置服务，第一版必须与其他 Pi 内置文本服务一样展示并支持其 API Key 或 OAuth 能力。Pi 的 OAuth 流程允许输入 GitHub Enterprise 域名，因此 WorkLens 的通用认证界面也必须承载该输入和设备码流程。企业域名只是 Pi 已声明的通用认证输入，不是针对某个客户或某个 GitHub Enterprise 实例的产品定制。

某个 GitHub Enterprise 实例是否已经通过真实环境测试，只决定“WorkLens 已验证”标签和帮助文案，不决定 GitHub Copilot 是否进入支持列表。若企业环境实测发现端点、请求头或模型筛选不兼容，应记录明确阻断并优先在 Pi provider 层修复；WorkLens 不为任何特定企业实例复制专用模型协议。

---

## 十三、安全与隐私

### 13.1 固定安全要求

1. `contextIsolation: true`。
2. `nodeIntegration: false`。
3. `sandbox: true`。
4. 网络、文件、命令行、凭据和会话操作只在主进程执行。
5. 渲染进程常规状态中没有明文凭据。
6. 本地工具只能由主进程中的 Pi 智能体运行链路发起，渲染进程不能直接调用。
7. 文件与命令工具可访问当前操作系统用户有权访问的范围；第一版不提供 WorkLens 路径沙箱、权限模块或代码级逐次授权。重要写入只通过系统提示词和工具描述要求对话确认。
8. `~/.worklens/runtime` 只是统一 `runtimeCwd`，不能作为文件访问边界、安全声明或 session 工作区；`~/.worklens/sessions` 是内部会话记录目录，也不是工具沙箱。
9. 首次使用本地工具前明确说明：写入与命令在模型发起后会直接执行；提示词中的确认约定不是沙箱或权限模块。命令行不是沙箱。
10. 不自动加载 `~/.worklens/runtime`、`~/.worklens/sessions`、项目目录或其他位置的第三方扩展、技能、提示词、`AGENTS.md` 或 `CLAUDE.md`。
11. 不采集提示词、工具结果和凭据的产品遥测。

### 13.2 数据发送说明

首次配置并开始使用本地工具前，必须说明：

- 用户消息、系统提示词、相关工具结果和读取到的文件内容会发送给用户选择的模型服务商。
- 智能体可以读取、创建、修改当前操作系统用户有权访问的文件，也可以执行命令并产生本机或网络副作用。
- 第一版没有 WorkLens 自身的路径沙箱、权限模块和代码级逐次授权；Electron 渲染进程沙箱不限制主进程工具的操作系统权限。智能体被要求对重要修改先对话确认，但该约定不能阻止已发起的工具执行。
- WorkLens 不会把用户凭据发送给其他模型服务商。

### 13.3 提示词注入

第一版必须把本地文件内容视为不可信输入：

- WorkLens 不把本地文件自动加载为系统提示词、扩展、技能或其他高优先级指令。
- 固定系统提示词要求智能体不要主动读取与任务无关的凭据、环境变量或敏感文件，并在修改比较重要的内容前先对话确认；第一版不得把该提示词描述成代码级权限保护。
- 文件内容可能影响模型后续工具调用；风险说明必须如实指出第一版没有权限模块。
- 未来引入权限模块后，每次具体操作都必须经过独立策略判断，不能依靠目录、session 或提示词约定作为权限边界。
- 工具返回的错误不得泄露凭据或无关绝对路径。

### 13.4 日志与诊断

- 默认日志不得包含完整用户消息、文件内容、接口密钥或授权令牌。
- 错误信息记录服务商、模型、阶段、状态码和脱敏提示。
- 用户主动导出诊断信息时，应先展示将包含的字段。

---

## 十四、非功能需求

### 14.1 可靠性

- 模型服务失败不能导致应用崩溃。
- 工具错误进入智能体上下文，允许模型恢复。
- 窗口关闭时取消全部活跃运行；取消或销毁单个 session 时，不能影响其他 session，也不能启动该 session 尚未开始的工具或残留失去归属的子进程。
- 带工具调用的助手消息必须在相应工具执行前持久化。
- 工具结果完成后立即持久化，不等待整次运行结束。
- Pi 无法识别或打开单个会话文件时，错误不能阻止其他会话加载；WorkLens 不额外实现第二套会话校验器。
- 并发流式事件、工具结果、错误和取消状态不能串入其他 session。

### 14.2 性能

- 基准开发机冷启动到可交互目标为 3 秒内，不含模型网络刷新。
- 主进程收到流式增量后，界面在 100 毫秒内完成可见更新。
- 500 个本地会话的列表在 2 秒内首次显示。
- 长工具输出必须有界，不能冻结渲染进程。
- 流式更新应按帧或短时间窗口合并，避免每个字符触发完整页面重绘。
- 至少两个 session 同时流式生成并执行工具时，界面切换、输入和停止操作仍保持可交互；不得因为后台 session 的增量更新重绘全部会话内容。

### 14.3 可访问性与交互

- 键盘可完成发送、停止和切换会话。
- 本地工具风险说明可通过键盘访问，不能只依靠颜色或短暂提示表达。
- 仅使用颜色不能成为表达工具状态的唯一方式。
- 浅色与深色主题均达到可读对比度。
- 长路径、长命令和长模型名称有完整查看方式。

### 14.4 平台

- macOS：Apple Silicon 为最高优先级，Intel 构建策略在发布前确认。
- Windows：x64 为最高优先级。
- macOS 使用 Bash；Windows 优先 PowerShell。
- Windows 是否额外支持 Git Bash，由技术预研决定。

### 14.5 可维护性

- WorkLens 不包含模型服务专用的流式解析器。
- WorkLens 不包含自定义智能体循环。
- 进程通信契约在共享类型中定义，并接受编译期检查。
- Pi 事件到界面事件的转换具有单元测试。
- 活跃运行注册表、事件路由和取消使用 `conversationId` 与 `runId` 双重校验，并覆盖多 session 并发测试。
- 模型服务成员关系、认证和模型能力直接来自 Pi；设置页使用通用认证交互和轻量 `ProviderPresentationOverride`，不膨胀为按服务商分支的巨型组件。
- 全局 `runtimeCwd`、工具注册和 session 生命周期分别测试，任何一项都不得以目录字段耦合另外两项。
- 未来权限模块只能插入主进程统一工具执行入口，不得要求重写 session 模型或按目录重新注册工具。第一版不得把系统提示词或工具描述中的确认约定实现成该策略层。

---

## 十五、开发前必做技术预研

以下预研必须在正式功能开发前完成，结论写入架构决策记录。

### 预研一：Electron 打包兼容性

验证内容：

- Electron 主进程可以导入并运行 `@earendil-works/pi-coding-agent`。
- `createAgentSession()`、基础工具和上下文压缩可以运行。
- macOS 与 Windows 打包后没有 ESM、ASAR、工作线程、WASM 或可选原生依赖问题。
- Pi 的 Node 版本要求与 Electron 内置 Node 运行时实际兼容。
- 开发模式和安装包模式都能找到必要资源。

通过条件：两平台安装冒烟测试通过。若失败，优先推动 Pi 提供更轻的公开软件包，再考虑采用 `pi-agent-core` 组合方案。

### 预研二：凭据存储与模型服务

验证内容：

- 基于 `safeStorage` 的 `CredentialStore` 可以读取、列出、修改和删除凭据。
- 并发 OAuth 刷新不会覆盖新令牌。
- Pi 内置文本服务商和模型可以通过 `ModelRuntime` 完整枚举，WorkLens 不维护成员白名单。
- 通用认证界面可以承载 Pi `AuthInteraction` 的文本、密码、选择、手工代码、OAuth 地址、设备码、进度和取消。
- Azure 基础地址和部署映射可用。
- 至少选择一个接口密钥服务、一个 OAuth 或设备码服务和一个多字段或环境凭据服务，完成真实的最小文本请求与工具调用。
- 渲染进程、日志和错误中没有秘密值。

通过条件：所有 Pi 内置文本服务都能从同一公开接口显示和发起其声明的认证流程；代表性认证路径通过真实端到端测试；未实测服务正确显示验证状态但不被隐藏。

### 预研三：会话与崩溃持久化

在以下时点强制终止进程并检查会话：

- 用户消息提交后、首个助手增量前。
- 工具调用消息完成后、工具执行前。
- 工具产生副作用后、工具结果写入前。
- 工具结果完成后、最终回答前。
- 上下文压缩过程中。

目标是明确 Pi 原生保证和 WorkLens 必须补充的恢复策略。任何“副作用已发生但没有会话记录”的窗口都是发布阻断项。

同时验证多 session 并发隔离：

- session A 流式生成期间，session B 可以发送并独立流式生成。
- 两个 session 的消息、工具调用、压缩、重试和错误只写入各自 JSONL 文件。
- 停止、删除或销毁 session A 时，session B 继续运行。
- 两个 session 同时调用工具时，事件和结果不会串线；同时写同一文件时遵循需求 055 的路径互斥与冲突语义。
- 应用退出时，所有活跃运行均被取消并分别完成可恢复的状态收束。

通过条件：至少两个 session 连续并发运行、交错切换、分别停止和恢复的专项测试通过，且不存在跨 session 消息污染、误取消或持久化串写。

### 预研四：全局运行目录与操作系统范围工具

验证内容：

- macOS 与 Windows 首次启动都能在用户主目录下创建并复用 `.worklens/runtime` 与 `.worklens/sessions`。
- 所有新建和恢复的 Pi 会话都使用同一个规范化 `runtimeCwd`，切换 session 不改变工具定义或运行目录。
- 相对文件路径和 Shell 初始目录落在 `~/.worklens/runtime`；绝对路径可以访问该目录之外、当前操作系统用户有权访问的位置。
- Windows 的其他磁盘与网络路径、macOS 用户有权访问的外部路径均按原生路径语义工作。
- 打开带有其他 `cwd` 的历史 Pi 会话时，可以通过公开能力覆盖或兼容迁移到当前全局值。
- `DefaultResourceLoader` 不会因为 `runtimeCwd` 位于 `~/.worklens/runtime` 而自动加载其中、`~/.worklens/sessions` 或其父目录中的扩展、技能、提示词和上下文文件。
- 取消后尚未开始的工具不会启动；已经完成的副作用和工具结果保持可追踪。

通过条件：两平台的根目录、运行目录和会话目录初始化、跨目录访问、session 切换、历史恢复、资源关闭和取消测试全部通过，且产品中不存在目录选择入口或逐 session 工作目录状态。

### 预研五：GitHub Copilot 企业域名实测

该预研不阻塞第一版。它验证 Pi 对 GitHub Copilot 的 GitHub Enterprise 域名 OAuth 是否兼容，可用任意可访问的企业实例，不为特定客户或特定域名建立专用路径。验证内容包括：

- 用户输入的 GitHub Enterprise 域名与设备码流程。
- Copilot 令牌端点。
- 企业实例所需请求头。
- 模型列表与文本响应。
- 推理等级。

通过后记录“WorkLens 已验证”状态和企业环境帮助文案；不通过时保留 GitHub Copilot 服务入口、显示明确阻断，并将兼容修复放在 Pi provider 层。该结果不改变由 Pi 决定的服务商支持集。

---

## 十六、开发里程碑

### 里程碑零：架构验证

- 完成预研一至四。
- 固定 Pi 版本。
- 确认会话和凭据事实源。
- 形成智能体运行时、全局 `runtimeCwd` 与工具作用域、进程通信三份架构决策记录。

退出条件：所有发布阻断风险都有实测结论。

### 里程碑一：桌面外壳

- 建立新的 Electron 项目结构。
- 建立主进程、预加载桥和渲染进程边界。
- 完成会话侧边栏、聊天空状态和设置页骨架。
- 完成主题、错误边界、`~/.worklens/runtime` 与 `~/.worklens/sessions` 初始化和本地工具风险说明。

退出条件：安装包可以启动，设置与空会话流程可操作。

### 里程碑二：模型服务与纯对话

- 完成凭据存储。
- 完成 Pi 内置文本服务商的通用设置、认证状态和测试连接。
- 完成文本流式对话、推理摘要、取消和错误展示。
- 完成会话新建、恢复、重命名和删除。
- 完成按 session 隔离的活跃运行注册表和并发流式事件路由。

退出条件：Pi 内置文本服务商均由 Pi 目录驱动显示且不存在 WorkLens 白名单；至少一个接口密钥服务和一个 OAuth 或设备码服务完成端到端纯对话闭环；至少两个 session 可以同时流式生成，停止其中一个不影响另一个。

### 里程碑三：本地智能体

- 接入文件搜索与读取工具。
- 接入写入和命令行工具。
- 完成与 session、任务路径无关的产品级工具注册。
- 完成工具时间线和差异展示。
- 完成跨目录访问、session 内副作用顺序执行、跨 session 工具并发、同文件互斥和精确取消。

退出条件：真实跨目录任务中的读取、修改和命令执行均通过测试；切换 session 不改变 `runtimeCwd` 或工具集；两个 session 的并发工具事件、结果和取消互不串线。

### 里程碑四：稳定与发布

- 完成上下文压缩、重试和异常恢复。
- 完成 macOS 与 Windows 打包。
- 完成 provider 目录同步、通用认证交互和“WorkLens 已验证”状态的发布检查。
- 完成安全、性能和试用验收。

退出条件：第十七节全部发布验收项通过。

---

## 十七、发布验收标准

### 17.1 桌面闭环

- [ ] macOS 与 Windows 安装包可以在干净环境启动。
- [ ] 无可用模型服务时显示可完成的首次引导。
- [ ] 至少配置一家服务后可以创建会话并获得流式回复。
- [ ] 设置、会话和聊天之间切换不会丢失状态，离开当前视图不会暂停后台 session 的活跃运行。
- [ ] 重启后可以继续历史会话。

### 17.2 智能体闭环

- [ ] 不选择工作目录即可完成一次包含 `find`、`grep` 和 `read` 的多轮任务。
- [ ] session A 运行期间可以切换到 session B 并立即发送消息，两个 session 均持续流式生成。
- [ ] 同一个 session 不能同时存在两个活跃运行；不同 session 不受全局忙碌状态或单运行锁限制。
- [ ] 停止、删除或销毁 session A 只结束其对应 `runId`，session B 的模型请求、工具和界面状态保持运行。
- [ ] 并发 session 的消息、工具事件、错误、重试和持久化记录不存在串线。
- [ ] 所有 session 使用同一个 `~/.worklens/runtime` `runtimeCwd`，切换 session 不改变工具集。
- [ ] 所有 Pi JSONL 会话记录写入 `~/.worklens/sessions`，不再写入 Electron `userData/sessions`。
- [ ] `read`、`write`、`edit` 和搜索工具可以通过绝对路径访问 `~/.worklens/runtime` 之外、当前操作系统用户有权访问的位置。
- [ ] 命令行工具以 `~/.worklens/runtime` 为初始目录，并可在命令中切换到其他可访问路径。
- [ ] 文件和命令操作的参数、状态、结果与副作用在工具时间线中可追踪。
- [ ] 不同 session 可同时执行工具；同一目标文件的并发 `write` 或 `edit` 按路径互斥，并产生可理解的等待或冲突结果。
- [ ] 取消模型请求或工具能在有限时间内结束运行，取消后尚未开始的工具不再启动。
- [ ] 上下文压缩后能够继续对话并在重启后恢复。

### 17.3 模型服务

- [ ] 当前固定 Pi 版本的全部内置文本服务商由 `ModelRuntime` 目录驱动显示，不存在 WorkLens 手工成员白名单。
- [ ] 通用认证界面覆盖 Pi 声明的全部 `AuthInteraction` 提示和事件类型。
- [ ] 至少一个接口密钥服务和一个 OAuth 或设备码服务在 macOS 与 Windows 安装包中通过端到端测试。
- [ ] 每个显示“WorkLens 已验证”标签的服务都具备第十二节要求的完整验证证据。
- [ ] Azure 自定义部署名称通过真实测试。
- [ ] 推理摘要界面与模型能力一致。
- [ ] 凭据和模型错误均通过脱敏测试。

### 17.4 安全

- [ ] 渲染进程无法直接访问 Node、Electron、文件系统或模型服务。
- [ ] 渲染进程常规接口不返回明文凭据。
- [ ] 产品明确说明本地工具具有当前操作系统用户权限，`~/.worklens/runtime` 不是访问边界或沙箱。
- [ ] 第一版没有权限模块、授权对话框或 `waiting_authorization`；界面不提供逐次放行控件，也不把对话确认描述成权限保护。
- [ ] 固定系统提示词以及 `write`、`edit` 的工具描述包含“重要修改应先征求用户确认”的行为指引。
- [ ] `~/.worklens/runtime`、`~/.worklens/sessions`、项目目录和其他本地位置中的扩展、技能、提示词、`AGENTS.md` 与 `CLAUDE.md` 不会被自动加载。
- [ ] 命令行风险说明明确其可以访问其他路径、启动进程和访问网络，且不具备沙箱隔离。

### 17.5 质量门

- [ ] 类型检查、单元测试和生产构建全部通过。
- [ ] Pi 依赖版本被固定并记录。
- [ ] 打包产物在干净机器完成安装冒烟测试。
- [ ] 浅色与深色主题完成手动检查。
- [ ] 发布包只包含本文档定义的运行时和产品模块。
- [ ] 模型请求、智能体循环、会话和基础工具均通过 Pi 公开接口实现。
- [ ] 软件包许可证、第三方声明和版本锁定文件齐全。

---

## 十八、成功指标

第一版不引入行为遥测，指标通过内部试用和结构化测试收集。

### 18.1 产品指标

- 5 名试用用户中，至少 4 名能在 10 分钟内独立完成模型服务配置和首次智能体任务。
- 试用用户累计完成至少 20 个真实任务，其中 80% 不需要重启应用或手工修复会话。
- 用户能正确解释 `~/.worklens` 是数据根目录，`~/.worklens/runtime` 只是初始运行目录，二者都不是工作区、权限边界或命令沙箱；对话中的确认不是权限模块。
- 用户可以在一个 session 运行期间切换到另一个 session 开始新任务，并能从侧边栏正确识别多个运行状态。
- 用户重启应用后能够在 30 秒内找到并继续最近会话。

### 18.2 工程指标

- Pi provider 与模型目录同步契约测试通过率为 100%，WorkLens 自维护的 provider 成员白名单数量为 0。
- 标记为“WorkLens 已验证”的服务，其完整验证矩阵通过率为 100%。
- 跨 session 的 `runtimeCwd` 和工具注册差异数量为 0。
- 20 轮双 session 并发流式与工具测试中的跨会话事件、消息和持久化污染数为 0。
- 对任一 session 执行停止后，其他活跃 session 被误取消数为 0。
- 取消后新启动的所属工具数和残留失去归属的子进程数为 0。
- 30 次连续会话重启恢复测试通过率为 100%。
- 500 个会话的列表性能达到第十四节目标。

---

## 十九、数据兼容与发布策略

### 19.1 数据命名空间

- 应用使用固定且唯一的应用标识。
- 正式版使用用户主目录下的 `~/.worklens` 作为 WorkLens 数据根目录，其中 `runtime/` 是所有 session 共用的 `runtimeCwd`，`sessions/` 存放 Pi JSONL 会话文件；不得在根目录直接散放会话文件，也不按 session 创建运行目录。
- Electron `userData` 只存放应用状态、加密凭据、模型缓存和日志，不再存放 session 历史。
- 工具使用相对路径创建的文件位于 `~/.worklens/runtime`，不会与 `~/.worklens/sessions` 的内部记录混放。
- 卸载、升级和清理缓存不得自动递归删除 `~/.worklens`。
- 开发、内部测试和正式环境使用不同的数据根目录；正式版固定为 `~/.worklens`，非正式渠道使用带渠道后缀的同级目录或显式测试目录。
- 自动化测试只使用临时数据目录，不读写用户真实数据。

### 19.2 首版数据输入范围

第一版不提供产品级 session 导入界面或第三方格式兼容层。`~/.worklens/sessions` 中的文件直接交给 Pi `SessionManager` 发现和读取：手工放入、编辑或破坏文件属于非受管操作，其识别、跳过、迁移或报错行为以当前固定 Pi 版本为准，WorkLens 不增加独立校验和自动修复。这不限制智能体通过本地工具读取用户明确要求处理的操作系统文件。

第一版不承诺以下数据可以导入：

- 其他应用的会话记录。
- 非 Pi 当前格式的 SQLite 或 JSONL 会话文件。
- 外部凭据和模型设置。
- 长期记忆、工作焦点或企业工具数据。

未来增加导入能力时，必须另立需求文档，定义来源格式、冲突处理、失败回滚和用户确认流程。

### 19.3 版本兼容与回退

- WorkLens 自有持久化文件包含格式版本；Pi JSONL 的版本、解析和迁移由当前固定 Pi `SessionManager` 管理。
- WorkLens 自有数据升级只能执行前向、幂等的迁移，不得静默降级或丢弃未知字段；会话文件不增加 WorkLens 自定义迁移层。
- Pi 依赖升级必须作为独立变更，完成会话兼容和安装包测试后发布。
- 新版本启动失败时，安装器允许回退应用程序文件；用户数据保持不变。
- 如果 Pi 无法发现或打开某个会话文件，WorkLens 捕获并展示 Pi 返回的脱敏诊断，不建立隔离副本、校验索引或自动修复流程；Pi 自身的跳过无效行、补全换行和版本迁移等原生行为不由 WorkLens 重复实现。
- 正式发布采用内部测试、小范围试用和逐步放量三个阶段。

---

## 二十、后续路线图

### 后续小版本：补齐桌面效率

- 会话固定、搜索和导出。
- 图片附件。
- 补充更多 Pi 内置服务商，以及 GitHub Copilot 企业域名路径的真实环境验证状态与帮助文案。
- 自定义 OpenAI 兼容模型服务。
- 独立于 `runtimeCwd` 和 session 的权限模块，包括代码级逐次确认、持久规则、敏感路径策略与完整审计。第一版的系统提示词约定不替代该模块。

### 第二阶段：可扩展智能体

- WorkLens 内置技能。
- 用户可控的技能和扩展。
- 提示词模板。
- 会话分支。
- 可选独立伴随进程或沙箱。

### 第三阶段：工作助理差异化

- 长期记忆和用户发现。
- 会话搜索和会话摘要。
- Jira、GitHub、Confluence 与 Tavily。
- 工作焦点和后台巡视。
- 跨工具工作流。

只有在第一版运行时、会话和工具执行模型达到发布标准后，第三阶段能力才可以进入请求主链路。

---

## 二十一、主要风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| `pi-coding-agent` 依赖较重 | 安装包体积、启动或原生依赖问题 | 优先完成打包预研；只使用公开接口；必要时推动轻量软件包 |
| Pi 公开接口快速变化 | 升级破坏会话、事件或工具 | 固定版本、契约测试、独立升级变更 |
| 模型服务多且认证方式不统一 | 设置页再次膨胀或目录与 Pi 漂移 | 由 Pi `ModelRuntime` 决定成员关系，使用通用 `AuthInteraction` 和轻量 `ProviderPresentationOverride` |
| Azure 部署与模型目录不一致 | 连接成功率低 | 明确部署映射并使用真实请求验证 |
| 文件与命令工具拥有较大本地访问范围 | 可能读取敏感内容、修改文件或产生系统与网络副作用 | 首次使用前明确风险；系统提示词与 `write`/`edit` 工具描述要求重要修改先确认；工具时间线完整可见；可靠停止；后续专门设计独立权限模块和隔离方案 |
| 多 session 并发运行 | 事件串线、误取消、服务商限流、资源占用升高或两个任务修改同一资源 | `conversationId` 与 `runId` 双重路由、session 级状态机、同文件短期互斥、并发专项测试和每 session 独立错误呈现 |
| JSONL 会话列表变慢 | 大量会话时启动变慢 | 先测 500 个会话，必要时增加可重建索引 |
| `runtimeCwd` 被误实现为工作区 | session 再次与目录绑定，跨目录工具被错误裁剪 | 固定 `~/.worklens/runtime`、删除目录选择入口，并对跨 session 与跨目录行为做契约测试 |
| `~/.worklens/sessions` 被手工修改 | Pi 可能跳过无效行、拒绝打开或按原生迁移重写，会话历史可能不完整 | 明确该操作不受管，直接依赖固定 Pi 版本的检测与诊断行为，不增加 WorkLens 自动修复逻辑 |
| Pi 持久化存在崩溃窗口 | 副作用与会话记录不一致 | 发布阻断预研，必要时增加轻量运行日志 |
| GitHub Copilot 企业环境不兼容 | 部分 GitHub Enterprise 实例无法登录或调用 | 保留 Pi 内置 GitHub Copilot 入口并显示明确阻断；专项实测；在 Pi provider 层修复 |
| 首版范围持续扩张 | 核心闭环延期、质量下降 | 以第六节为范围基线；新增能力必须调整里程碑和验收标准 |

---

## 二十二、待确认决策

以下问题不阻塞本文档定稿，但必须在对应里程碑前决定：

1. 首批为哪些 Pi 内置服务获取真实测试账户并授予“WorkLens 已验证”标签；缺少测试条件不影响服务进入支持列表。
2. macOS Intel 是否与 Apple Silicon 同期发布。
3. Windows 只支持 PowerShell，还是同时支持 Git Bash。
4. 测试版的产品名称与应用标识。
5. 会话永久删除是否增加短期回收站。
6. 引入 Pi 后的安装包体积预算。

---

## 二十三、交付追踪矩阵

| 交付结果 | 对应需求 | 主要验收依据 |
|---|---|---|
| 可安装桌面应用 | 001、002、060、063 | 17.1、17.5 |
| 模型服务配置闭环 | 010—016 | 12.1、17.3 |
| 全局运行目录与 session 解耦 | 020、021 | 17.2、17.4 |
| 本地持久会话与跨 session 并发 | 030—035 | 10.4、11.1、14.1、17.1、17.2 |
| 流式智能体对话 | 040—047 | 14.2、17.2 |
| 操作系统范围的文件与命令工具 | 050—056 | 13.1、17.2、17.4 |
| 凭据与进程隔离 | 012、061—063 | 13.1、17.4 |
| 崩溃恢复与数据兼容 | 033、044—046、063 | 11.5、14.1、19.3 |
| 两平台发布包 | 全部最高优先级需求 | 14.4、17.5 |

每个实现任务和测试用例至少关联一项需求编号。无法关联的工作不属于第一版范围，除非产品需求文档先完成变更。

---

## 二十四、完成定义

WorkLens 第一版只有在以下条件全部满足时才算完成：

- 产品只使用一套 Pi 智能体运行时；模型流、智能体循环、会话和基础工具均通过 Pi 公开接口提供。
- 用户可以从首次启动完成模型配置、创建会话和一次真实智能体任务。
- 会话、模型状态、工具调用、工具结果和上下文压缩可以在重启后恢复。
- 不同 session 可以并发运行；同一 session 保持单一活跃运行，切换、停止或删除一个 session 不影响其他 session。
- 所有 session 共享 `~/.worklens/runtime` `runtimeCwd`，历史记录存放于 `~/.worklens/sessions`；session 不包含 WorkLens 产品级工作目录字段，也不按目录注册工具。
- 文件与命令工具可在当前操作系统用户权限范围内直接工作；产品明确说明第一版没有权限模块、代码级逐次授权或目录沙箱，仅通过系统提示词和工具描述对重要写入提出对话确认要求。
- macOS 和 Windows 安装包均通过干净环境安装测试。
- Pi 内置文本服务商全部由 Pi 目录驱动显示，且所有“WorkLens 已验证”标签都有完整验证证据。
- 取消后该 run 尚未开始的工具不再启动，其他 session 不被误取消，且不存在失去运行归属的残留子进程。
- WorkLens 自有数据具有明确格式版本；Pi 无法识别或打开的单个会话文件不会影响其他会话和应用启动。
- 第十七节所有验收项均通过。
